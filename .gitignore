node_modules/
dist/
build/
.env
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/


*.log
*.err

.DS_Store
letsencrypt/

# ── Test artifacts ───────────────────────────
.auth_state.json
test-results.xml
.mypy_cache/
.ruff_cache/
# ── Python build artifacts (pip install -e) ──
*.egg-info/

# Документация (/docs/) и агентная система (/backend/src/agents/) теперь в git:
# сервер получает код копированием репозитория, база знаний нужна агентам там.

# Local-only compose overlay (MailHog и пр.) — не выкатываем на сервер
docker-compose.override.yml

# Alertmanager Telegram bot token (real secret) — only the .example is committed
/monitoring/alertmanager/bot_token
