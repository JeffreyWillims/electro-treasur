# assets

Статические изображения, импортируемые компонентами напрямую (обрабатываются Vite).

- `hero.png` — иллюстрация для приветственного/hero-экрана.
- `react.svg`, `vite.svg` — логотипы стартового шаблона Vite+React (не используются в UI приложения).
