# components/profile

- `ProfileSettings.tsx` — страница профиля: редактирование данных пользователя,
  привязка Telegram-бота (OTP через `generateTelegramOtp`), панель запуска
  проверки кода агентами (`AgentVerifyPanel`), выход из аккаунта.
