# public

Статические файлы, отдаваемые Vite как есть (без обработки сборщиком), доступны по корневому пути.

- `favicon.svg` — иконка вкладки браузера.
- `icons.svg` — спрайт SVG-иконок приложения (переиспользуемые `<use href="#...">`).
