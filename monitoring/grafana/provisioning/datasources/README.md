# monitoring/grafana/provisioning/datasources

Автоматически подключаемые источники данных Grafana.

## Файлы

- **prometheus.yml** — регистрирует Prometheus (`http://prometheus:9090`) как датасорс Grafana
  по умолчанию (`isDefault: true`).
