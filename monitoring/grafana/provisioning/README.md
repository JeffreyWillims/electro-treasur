# monitoring/grafana/provisioning

Автоматическая провизия Grafana при старте контейнера (без ручной настройки через UI).
Собственных файлов не содержит — датасорсы описаны в подпапке `datasources/` (см. её
`README.md`).
