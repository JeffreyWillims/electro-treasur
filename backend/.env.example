# ─────────────────────────────────────────────────────────────────────────────
#  .env.example — Electro-Treasur Backend Configuration Template
#
#  ИНСТРУКЦИЯ:
#  1. Скопируй этот файл: cp .env.example .env
#  2. Сгенерируй SECRET_KEY: python -c "import secrets; print(secrets.token_hex(32))"
#  3. Никогда не коммить .env в репозиторий (.gitignore уже защищает)
#
#  Все переменные используют префикс ET_ (env_prefix в pydantic-settings)
# ─────────────────────────────────────────────────────────────────────────────

# ── Окружение ────────────────────────────────────────────────────────────────
# local | staging | prod. В "prod" включаются инварианты безопасности: короткий
# ET_SECRET_KEY, ET_COOKIE_SECURE=false, пустой или localhost-CORS не дадут
# приложению стартовать (осознанный fail-fast вместо тихой дыры в бою).
ET_ENVIRONMENT="local"

# Origin'ы фронтенда для CORS, через запятую. В проде — реальные домены.
ET_CORS_ORIGINS="http://localhost:5173,http://localhost:3000"
# Прод: ET_CORS_ORIGINS="https://citrinevault.ru,https://www.citrinevault.ru"

# ── JWT Security (ОБЯЗАТЕЛЬНО — без этого приложение не запустится) ──────────
ET_SECRET_KEY="your-super-secret-256-bit-key-here-generate-with-secrets-token-hex-32"
ET_ALGORITHM="HS256"
# Secure-флаг cookie требует HTTPS. Локально по http — false, в проде true.
ET_COOKIE_SECURE=true

# ── PostgreSQL ────────────────────────────────────────────────────────────────
ET_DATABASE_URL="postgresql+asyncpg://electro:CHANGE_ME_STRONG_PASSWORD@localhost:5432/electro_treasur"

# ── Redis ─────────────────────────────────────────────────────────────────────
ET_REDIS_URL="redis://:CHANGE_ME_REDIS_PASSWORD@localhost:6379/0"
ET_ARQ_REDIS_URL="redis://:CHANGE_ME_REDIS_PASSWORD@localhost:6379/1"

# ── Optional tuning ───────────────────────────────────────────────────────────
# ET_DB_POOL_SIZE=20
# ET_DB_MAX_OVERFLOW=10
# ET_REDIS_INSIGHT_TTL=86400
# Хранилище rate-limiter. Пусто → используется ET_REDIS_URL (общие счётчики для
# всех воркеров). Локально/в тестах можно "memory://".
# ET_RATELIMIT_STORAGE_URI=""

# ── Обратная связь ────────────────────────────────────────────────────────────
# Кто видит окно «Обратная связь» в приложении (список email через запятую).
# Пусто → окно закрыто для всех, включая обычных пользователей.
# Письма больше не отправляются: обращения читаются прямо в интерфейсе.
ET_FEEDBACK_ADMIN_EMAILS="jackiejenks808@gmail.com"

# ── Telegram Bot ─────────────────────────────────────────────────────────────
ET_TELEGRAM_BOT_TOKEN=""

