# api/v2

Публичный API для внешних интеграций — авторизация по API-ключу (см. `v1/api_keys.py`),
без cookie-сессии.

- `__init__.py` — маркер пакета, пустой.
- `public.py` — публичные эндпоинты, доступные по API-ключу.
